<UserControl x:Class="PQM_V2.Views.HomeViews.StructuresLegendView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PQM_V2.Views.HomeViews" xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:converters="clr-namespace:PQM_V2.Converters"
             mc:Ignorable="d" 
             Background="#707070"
             d:DesignHeight="600" d:DesignWidth="400">
    <UserControl.Resources>
        <converters:InverseBooleanConverter x:Key="inverseBooleanConverter"/>
        <DataTemplate x:Key="myListViewItemTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140px"/>
                    <ColumnDefinition Width="10px"/>
                    <ColumnDefinition Width="20px"/>
                    <ColumnDefinition Width="10px"/>
                    <ColumnDefinition Width="100px"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Content="{Binding name}" Foreground="#ffffff" FontSize="15">
                </Label>
                <Rectangle Grid.Column="2" Fill="{Binding color}" Height="10" Width="20"/>
                <fa:ImageAwesome Grid.Column="4" Foreground="#cccccc" HorizontalAlignment="Right" Height="20px" Icon="Eye" Visibility="{Binding eye}" Width="20px"/>
                <fa:ImageAwesome Grid.Column="5" Foreground="#cccccc" Height="20px" Icon="EyeSlash" Visibility="{Binding eyeSlash}" Width="20px"/>
                <Grid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Show"/>
                        <MenuItem Header="Hide"/>
                        <MenuItem Header="Isolate"/>
                        <MenuItem Header="Delete"/>
                        <MenuItem Header="Select"/>
                    </ContextMenu>
                </Grid.ContextMenu>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <ListView ItemTemplate="{DynamicResource myListViewItemTemplate}" 
                  Style="{DynamicResource myListViewStyle}" Background="#707070" 
                  ItemsSource="{Binding LegendItemsList}">
        </ListView>
    </Grid>
</UserControl>
